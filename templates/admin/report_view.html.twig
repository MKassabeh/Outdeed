{% extends 'base_admin.html.twig' %}

{% block title %}Outdeed - Détails d'une offre{% endblock %}

{% block body %}
<div class="my-5 container blanc">
    <div class="card_container justify-content-around text-center">
        <div style="max-width: 800px !important;" class="card_body">
            <div class="head">
                <h1 class="mb-2 font_title">
                    {{ report.title|capitalize }}
                </h1>
            </div>
            <div class="d-flex">
                <div>
                    <p class="text_head"><strong>Description</strong>{{report.description}}</p>
                
            </div>
            <a class="nav-link" href="{{ path('admin_reported', {id: report.id}) }}">View Report</a>

</div>

{% endblock %}